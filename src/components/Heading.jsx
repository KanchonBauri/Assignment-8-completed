

const Heading = ({title, subtitle}) => {
    return (
        <div className="text-center">
            <h2>{title}</h2>
            <p>{subtitle}</p>
        </div>
    );
};

export default Heading;